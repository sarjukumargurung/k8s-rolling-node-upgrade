apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: database-pdb
  namespace: default
spec:
  # maxUnavailable: 0 will block drains. Prefer a replica count that
  # allows 1 disruption, or drain followers first.
  maxUnavailable: 1
  selector:
    matchLabels:
      app: database
